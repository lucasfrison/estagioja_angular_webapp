import { CandidaturaComEmpresa } from './candidatura-com-empresa.model';

describe('CandidaturaComEmpresa', () => {
  it('should create an instance', () => {
    expect(new CandidaturaComEmpresa()).toBeTruthy();
  });
});
