import { VagaComCandidatos } from './vaga-com-candidatos.model';

describe('VagaComCandidatos', () => {
  it('should create an instance', () => {
    expect(new VagaComCandidatos()).toBeTruthy();
  });
});
