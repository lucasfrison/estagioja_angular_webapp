import { EstudanteCandidato } from './estudante-candidato.model';

describe('EstudanteCandidato', () => {
  it('should create an instance', () => {
    expect(new EstudanteCandidato()).toBeTruthy();
  });
});
